check process solr
with pidfile /var/run/solr/solr-<%= @port %>.pid
  start program = "/engineyard/bin/solr start"
  stop program = "/engineyard/bin/solr stop"
  if totalmem is greater than <%= @memory_limit %> MB for <%= @memory_limit_cycles %> cycles then restart
  group solr
