version=1.8.1

[general]

# What to display in S mode: graph, status, pivoted status
S_func=s
# Which set of variables to display in S (Variables & Status) mode
S_set=general
# Whether to auto-wipe InnoDB deadlocks
auto_wipe_dl=0
# What type of characters should be displayed in queries (ascii, unicode, none)
charset=ascii
# Prefix for values in C mode
cmd_filter=Com_
# Whether to use terminal coloring
color=1
# Connection timeout for keeping unused connections alive
cxn_timeout=28800
# Debug mode (more verbose errors, uses more memory)
debug=0
# A debug file in case you are interested in error output
debugfile=
# Whether to put captions on tables
display_table_captions=1
# Whether to show GLOBAL variables and status
global=1
# Character for drawing graphs
graph_char=*
# How to highlight table column headers
header_highlight=bold
# Whether to show column headers
hide_hdr=0
# The interval at which the display will be refreshed.  Fractional values allowed.
interval=2
# Which mode to start in
mode=Q
# How many digits to show in fractional numbers and percents
num_digits=2
# How many sets of STATUS and VARIABLES values to show
num_status_sets=9
# Directory where plugins can be found
plugin_dir=
# Whether the config file is read-only
readonly=1
# Whether to print connection errors to STDOUT
show_cxn_errors=1
# Whether to display connection errors as rows in the table
show_cxn_errors_in_tbl=1
# Show the % symbol after percentages
show_percent=1
# Whether to show the status bar in the display
show_statusbar=1
# Disable SHOW INNODB STATUS
skip_innodb=0
# Whether to show raw or incremental values for status variables
status_inc=0
# The strftime() timestamp format to write in -n mode
timeformat=%Y-%m-%dT%H:%M:%S

[/general]

[plugins]


[/plugins]

[filters]


[/filters]

[active_filters]

processlist=hide_inactive

[/active_filters]

[tbl_meta]


[/tbl_meta]

[connections]

localhost=user= have_user= have_pass= dsn=DBI:mysql:;;mysql_read_default_group=mysql savepass= dl_table=

master=user= have_user= have_pass= dsn=DBI:mysql:;mysql_read_default_group=mysql;hostname=<%= @master_hostname %> savepass= dl_table=

<% @slaves_hostnames.each do |name, hostname| %>
<%= name %>=user= have_user= have_pass= dsn=DBI:mysql:;mysql_read_default_group=mysql;hostname=<%= hostname %> savepass= dl_table=
<% end %>

[/connections]

[active_connections]

B=localhost
C=localhost
D=localhost
F=localhost
I=localhost
L=localhost
M=
O=localhost
Q=localhost
R=localhost
S=localhost
T=localhost
U=localhost

[/active_connections]

[server_groups]

all=<%= @all_names %>
slaves=<%= @slave_names %>

[/server_groups]

[active_server_groups]

B=
C=
D=
F=
I=
L=
M=all
O=
Q=
R=
S=
T=
U=

[/active_server_groups]

[max_values_seen]

Com_delete=50
Com_insert=50
Com_select=50
Com_update=50
Questions=100

[/max_values_seen]

[active_columns]


[/active_columns]

[sort_cols]


[/sort_cols]

[visible_tables]


[/visible_tables]

[varsets]


[/varsets]

[colors]


[/colors]

[stmt_sleep_times]


[/stmt_sleep_times]

[group_by]


[/group_by]
