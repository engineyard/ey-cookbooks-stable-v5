# example settings for sending alert mails with monit.
# you can edit this file so we won't overwrite it.
# set mailserver smtp.gmail.com PORT 587
#     username '[user]@[domain]' password [pass]
#     using TLSV1
#     with TIMEOUT 20 seconds
# set mail-format {from:info@foo.com}
# set alert foo@bar.com only on { timeout, nonexist }

